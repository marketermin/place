'use client';
import {useState} from 'react';
import {login} from '@/app/actions';
import {ChartNoAxesCombined,ArrowRight,LockKeyhole} from 'lucide-react';
export default function Login({configured}:{configured:boolean}){const [error,setError]=useState('');const [busy,setBusy]=useState(false);return <main className="login-page"><section className="login-card"><div className="brand"><span className="brand-mark"><ChartNoAxesCombined/></span>플레이스 데이즈</div><div className="login-heading"><LockKeyhole size={28}/><h1>관리자 로그인</h1><p>우리 업장의 상위 노출을 차곡차곡 기록하세요.</p></div>{!configured&&<div className="info">실제 저장소 연결 전입니다. 아래 데모에서 화면과 계산을 체험할 수 있습니다.</div>}<form onSubmit={async e=>{e.preventDefault();setBusy(true);setError('');const fd=new FormData(e.currentTarget);try{const r=await login(String(fd.get('email')),String(fd.get('password')));if(r.error)setError(r.error);else window.location.assign('/manage');}catch{setError('연결하지 못했습니다. 잠시 후 다시 시도해 주세요.');}finally{setBusy(false);}}}><label>이메일<input name="email" type="email" autoComplete="username" required disabled={!configured}/></label><label>비밀번호<input name="password" type="password" autoComplete="current-password" required disabled={!configured}/></label>{error&&<p role="alert" className="error">{error}</p>}<button className="primary full" disabled={busy||!configured}>{busy?'로그인 중…':'로그인'}<ArrowRight size={18}/></button></form><a className="demo-link" href="/demo">데모 체험하기 <ArrowRight size={16}/></a><p className="small">초대된 관리자만 이용할 수 있습니다. 비밀번호를 잊었다면 Supabase 관리자에게 재설정을 요청해 주세요.</p></section></main>;}
