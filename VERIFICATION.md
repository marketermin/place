# 검증 결과

2026-09-19, 이 컴퓨터에서 실행했습니다.

## 완료한 검사

- Next.js 16.3.5 실제 실행 및 `/demo` HTTP 200 응답.
- TypeScript 검사 통과, 실제 배포용 빌드 통과.
- 자동 테스트 6개 통과: 날짜/계산 5개, 실제 PostgreSQL 코드 실행을 포함한 저장·권한 통합 시험 1개.
- 같은 날짜 여러 번 저장해도 1개 기록 유지.
- 5위는 포함, 6위와 미확인은 제외.
- 한국 시간 자정 경계, 잘못된 날짜·미래 날짜 거부.
- 19일에서 알림 없음, 20일 알림 생성, 21~25일 단계별 고유 알림.
- 과거 순위 수정 시 잘못된 알림 제거, 도달 날짜 변경 시 읽음 상태 초기화.
- 목표 25일 이후 26일에도 초기화 없음.
- 관리 시작일 수정으로 재계산. 업장 삭제 시 관련 기록·검색어·알림 정리.
- PostgreSQL 저장소를 종료하고 다시 열어도 기록 유지.
- 익명 사용자 읽기·쓰기 차단, 관리자가 아닌 로그인 사용자 쓰기 차단.
- 다른 관리자 소유의 업장·기록·알림 읽기/수정/삭제 차단.
- 클라이언트의 관리자 자가 등록 및 알림 조작 차단.
- 브라우저에서 업장 등록→새로고침→유지, 업장 수정과 삭제 직접 확인.
- 브라우저에서 오늘 순위 6위로 수정→22일에서 21일, 알림 감소→새로고침 후 유지 확인. 5위로 바꿔 22일 복구 확인.
- 알림 읽음 처리 후 새로고침해 읽음 유지 확인.
- `/manage` 직접 접근 시 `/login`으로 이동 확인.
- 휴대폰 크기에서 입력 양식과 화면 확인. 표는 자체 좌우 스크롤로 표시.
- 마지막 수정 후 다시 빌드 성공. 휴대폰 390px에서 문서 폭 375px로 페이지 전체 가로 넘침 없음 확인. 1440px PC 화면도 확인. 브라우저 오류 로그 없음.

## 아직 확인할 수 없는 것

- 사용자 Supabase 프로젝트의 실제 이메일/비밀번호 로그인과 네트워크 저장: 계정 연결 전이라 미검증입니다.
- Vercel 공개 배포: 로컬 링크 요청에 따라 배포하지 않았습니다.
- 자동 순위 수집: 연결되어 있지 않습니다. `RESEARCH.md`의 검증 후 진행해야 합니다.
- 이메일 알림: 첫 버전 범위 밖이며 구현되지 않았습니다.

권한·저장 테스트는 PGlite PostgreSQL에서 동일한 SQL 함수를 실제 실행했습니다. Supabase Auth의 호스팅된 로그인까지 통과했다는 뜻은 아닙니다. 데모의 브라우저 저장은 실제 Supabase 저장과 구분합니다.
