NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Gemini is optional. Keep this key server-only; never prefix it with NEXT_PUBLIC_.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# Automatic Naver rank collection stays disabled until a provider's collection
# and retention permission, measurement rules, and endpoint are verified.
RANK_PROVIDER_ENABLED=false
RANK_PROVIDER_BASE_URL=
RANK_PROVIDER_API_KEY=
ADMIN_EMAIL=
ADMIN_PASSWORD=
